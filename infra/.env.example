# ============================================================================
# sports-data-admin Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values.
# This single .env file works for API, scraper, and web (Next.js will read it).
# ============================================================================

# ----------------------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------------------
# For connecting to your existing dock108 database with sports data:
DATABASE_URL=postgresql+asyncpg://dock108:YOUR_PASSWORD@localhost:5432/dock108

# For Docker Compose (only needed if running postgres in Docker):
POSTGRES_DB=dock108
POSTGRES_USER=dock108
POSTGRES_PASSWORD=YOUR_PASSWORD

# ----------------------------------------------------------------------------
# Redis Configuration (for Celery task queue)
# ----------------------------------------------------------------------------
REDIS_URL=redis://:YOUR_REDIS_PASSWORD@localhost:6379/2
REDIS_PASSWORD=YOUR_REDIS_PASSWORD

# Alternative: No password (local dev without auth)
# REDIS_URL=redis://localhost:6379/2

# Celery (optional - defaults to REDIS_URL if not set)
# CELERY_BROKER_URL=redis://:YOUR_REDIS_PASSWORD@localhost:6379/2
# CELERY_RESULT_BACKEND=redis://:YOUR_REDIS_PASSWORD@localhost:6379/2

# ----------------------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------------------
# Odds API (optional - for fetching betting odds)
ODDS_API_KEY=YOUR_ODDS_API_KEY

# Safari Instructions:
# 1. Log into x.com in your browser
# 2. Open DevTools > Application > Cookies > x.com
# 3. Copy the values of 'auth_token' and 'ct0' cookies
# X/Twitter authentication for social scraping
X_AUTH_TOKEN=paste_auth_token_value_here
X_CT0=paste_ct0_value_here

# Environment
ENVIRONMENT=development

# SQL logging (set to true for debugging)
SQL_ECHO=false

# ----------------------------------------------------------------------------
# Frontend Configuration (NEXT_PUBLIC_* are embedded in browser bundle)
# ----------------------------------------------------------------------------
# URL where the sports-data-admin API is running
NEXT_PUBLIC_SPORTS_API_URL=http://localhost:8000

# Legacy compatibility (some code checks this too)
NEXT_PUBLIC_THEORY_ENGINE_URL=http://localhost:8000
